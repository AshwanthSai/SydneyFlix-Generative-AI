import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    centerScreen: {
      position: "absolute",
      top: "50%",
      left: "55%",
      marginRight: "-50%",
      transform: "translate(-50%, -50%)",
    },
}));